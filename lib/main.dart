import 'package:flutter/material.dart';

void main() => runApp(Hungry());

class Hungry extends StatelessWidget {
  const Hungry({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
